tensorflow训练神经网络的最佳实践，结合来变量管理机制和模型持久化机制，将训练和测试分成两个独立的部分，这可以使每一个组件更加灵活，
将前向传播的过程抽象成一个单独的库函数，因为神经网络的前向传播过程在训练和测试的过程中都会用到，所以通过库函数的方式使用起来更加方便，又可以保证训练和测试过程中使用的前向传播的方法是一致的
重构的程序解决mnist问题，重构后代吗被拆成3个程序，第一个是mnistinference.py，他定义了前向传播的过程以及神经网络的参数，第二个是mnisttrain.py，他定义来训练的过程，第三个是mnisteval.py，他定义了测试过程
